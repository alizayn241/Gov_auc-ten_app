# TODO: Fix Errors in auctions_list_screen.dart

- [x] Add `refresh` method to `AuctionsViewModel` in `lib/features/auctions/presentation/viewmodel/auctions_view_model.dart`
- [x] Replace `state.isLoading` with `state.loading` in `auctions_list_screen.dart` (first occurrence)
- [x] Replace `state.isLoading` with `state.loading` in `auctions_list_screen.dart` (second occurrence)
- [x] Fix deprecated `withOpacity` usage in `auctions_list_screen.dart`
- [ ] Run `flutter analyze` to verify all errors are resolved
